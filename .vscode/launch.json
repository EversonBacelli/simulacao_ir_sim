{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Executar arvoreDFS",
            "type": "python",
            "request": "launch",
            "module": "simulation10_motionBFS_circular_ir_sim.execMotion",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
